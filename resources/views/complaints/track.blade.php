@extends('layouts.app') @section('content')<div class="center-page"><div class="track-card"><span class="eyebrow">COMPLAINT TRACKING</span><h1>Track Your Complaint</h1><p>Enter your complaint reference number.</p><form><input name="reference" value="{{ request('reference') }}" placeholder="CMP-202609-ABC123"><button class="submit">Track</button></form>@if(request('reference'))<div class="track-result">@if($complaint)<b>{{ $complaint->reference_no }}</b><span>{{ ucfirst(str_replace('_',' ',$complaint->status)) }}</span><small>Submitted {{ $complaint->created_at->format('d M Y, h:i A') }}</small>@else<span>No complaint found for this reference.</span>@endif</div>@endif</div></div>@endsection
