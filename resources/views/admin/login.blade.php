@extends('layouts.app') @section('content')<div class="center-page"><div class="track-card"><span class="eyebrow">ADMINISTRATION</span><h1>Admin Sign In</h1><p>Manage complaints and update their status.</p>@if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('admin.authenticate') }}">@csrf<label>Email<input type="email" name="email" required></label><label>Password<input type="password" name="password" required></label><button class="submit">Sign In</button></form></div></div>@endsection
