document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('textarea[maxlength]').forEach(t=>{const c=document.createElement('div');c.style='text-align:right;color:#8a97a9;font-size:10px;margin-top:4px';t.after(c);const u=()=>c.textContent=`${t.value.length} / ${t.maxLength}`;t.addEventListener('input',u);u()});document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',()=>{const b=f.querySelector('button[type="submit"],button.submit');if(b){b.disabled=true;b.style.opacity='.7';b.textContent='Submitting…'}}))});
